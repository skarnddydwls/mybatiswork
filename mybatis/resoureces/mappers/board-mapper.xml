<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="boardMapper">
	<resultMap type="Board" id="boardResultSet">
		<result column="board_no" property="no" />
		<result column="board_title" property="title" />
		<result column="board_content" property="content" />
		<result column="board_writer" property="writer" />
		<result column="count" property="count" />
		<result column="create_date" property="createDate" />
		<result column="status" property="status" />
	</resultMap>
	
	<resultMap type="Reply" id="ReplyResultSet">
		<result column="reply_no" property="no" />
		<result column="reply_content" property="content" />
		<result column="ref_bno" property="refBno" />
		<result column="reply_writer" property="writer" />
		<result column="create_date" property="createDate" />
	</resultMap>
	
	<select id="totalRecord" resultType="_int">
		select count(*)
		  from board
		 where status = 'Y'
	</select>
	<select id="selectList" resultMap="boardResultSet">
		select *
		  from board
		 where status = 'Y'
		 order by board_no desc 
	</select>
	<select id="selectSearchCount" resultType="_int">
		select count(*)
		  from board
		 where status='Y'
		 <if test="keyField=='writer'">
		 	and board_writer
		 </if>
		 <if test="keyField=='title'">
		 	and board_title
		 </if>
		 <if test="keyField=='content'">
		 	and board_content
		 </if>
		 like '%' || #{keyWord} || '%'
	</select>
	<select id="selectSearchList" resultMap="boardResultSet">
		select *
		  from board
		 where status='Y'
		 <choose>
		 	<when test="keyField=='writer'">
		 		and board_writer
		 	</when>
		 	<when test="keyField=='title'">
		 		and board_title
		 	</when>
		 	<otherwise>
		 		and board_content
		 	</otherwise>
		 </choose>
		 like '%' || #{keyWord} || '%'
		 order by board_no desc
	</select>
	
	<update id="countUp">
		update board
		   set count = count + 1
		 where board_no = #{boardNo}
		   and status = 'Y' 
	</update>
	
	<select id="selectBoard" resultMap="boardResultSet">
		select *
		  from board
		 where board_no = #{boardNo}
	</select>
	
	<select id="replyRecord" resultType="_int">
		select count(*)
		  from reply
		 where ref_bno = #{boardNo} 
	</select>
	
	<select id="selectReplyList" resultMap="ReplyResultSet">
		select *
		 from reply
		 where ref_bno = #{boardNo}
		 order by reply_no desc
	</select>
	
	<insert id="insertReply">
  		insert into reply values(seq_rno.nextval, #{content}, #{refBno}, #{writer}, default, default)
 	</insert>
 	
 	<insert id="insertBoard">
 		insert into board values(seq_bno.nextval, #{title}, #{content}, #{userId}, default, default, default)
 	</insert>
</mapper>
















